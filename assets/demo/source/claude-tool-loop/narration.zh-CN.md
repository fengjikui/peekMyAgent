# 《一次工具调用到底发生了什么》中文旁白审阅稿

本稿与 v0.2 网页故事板使用同一份时间线。故事板直接呈现底部单句字幕；以后导出视频时，仍可关闭或替换字幕，并单独加载目标语言 SRT 与授权配音。

- 总时长：约 04:05
- 字幕：51 条单句字幕，由时间线自动生成 SRT
- 画面语言：中文 Viewer / Claude 主题
- 当前配音：未开始；音色探索不阻塞中文故事和画面审阅
- Source：确定性 Anthropic Messages 教学轨迹；真实 CLI 交叉核对范围见 `manifest.json`
- 术语原则：`tool_use`、`tool_result`、System、Raw、Harness 保留英文写法

## 00:00–00:17　三个角色，完成一次工具调用

画面重点：用户提出目标；Claude Code 组织请求并在本地执行；远端模型选择下一步。

旁白：

这支视频只讲清一件事：用户、Claude Code 和远端模型，怎样共同完成一次工具调用。远端模型不会直接读取你的电脑；真正的本地执行发生在 Claude Code 这一侧。

## 00:17–00:38　先看完整闭环

画面重点：一个 Turn 内，两次模型请求串起用户目标、Read 调用、工具结果和最终回答。

旁白：

演示任务很简单：读取 README 第一行并告诉我项目名。PMA 在同一个 Turn 里，把用户目标、第一次模型请求、Read 工具调用、工具结果、第二次请求和最终回答串成一条证据链。

## 00:38–00:57　第一次请求调用了哪个模型？

画面重点：点击请求详情中的 Metadata，先核对模型、最大输出和流式参数。

旁白：

点开第一次请求的详情，再切到 Metadata。这里能核对模型、最大输出、是否流式、传输路径和上行构成。排查 Harness 时，先确认模型和参数没有在中间被改错。

## 00:57–01:17　模型实际收到了哪些 System 指令？

画面重点：System 区域展示该次请求中的真实字段，而不是根据模型行为反推。

旁白：

切到 System，可以看到这一次真正发送给模型的固定指令。演示轨迹明确限制为虚构的 demo 文件，不访问凭据、用户文件或网络。这里展示的是捕获证据，不是根据结果猜出来的提示词。

## 01:17–01:38　为什么模型会选择 Read？

画面重点：Claude Code 在请求中声明 Read、Glob、Bash；每个工具都有描述和参数 schema。

旁白：

Tools 区域给出了这一次模型可以选择的动作。这里同时声明了 Read、Glob 和 Bash，并附带描述与输入 schema。模型并不是预先写死要读文件；在这条 Capture 中，它从已提供的工具里返回了 Read。

## 01:38–01:59　模型提出调用，但还没有执行

画面重点：tool_use 只包含调用 ID、工具名和参数：README.md，从第一行读取一行。

旁白：

点击时间线里的 Read，右栏出现 tool use。模型返回了调用标识 read hello、工具名和参数：读取 README 点 m d，从第一行开始，只取一行。到这一步，模型只是提出调用，并没有亲自碰本地文件。

## 01:59–02:21　Claude Code 在本地执行并回传结果

画面重点：下一次请求中的 tool_result 引用同一 ID，真实输出是 # hello-agent。

旁白：

Claude Code 接到这个调用后，负责权限检查和本地执行。下一条工具结果显示，返回内容是井号 hello agent，并且引用同一个 read hello 标识。远端模型真正看到文件内容，是在后续请求收到 tool result 的时候。

## 02:21–02:42　结果晚出现，也能一键追溯来源

画面重点：点击“来源 #1”，PMA 直接跳回产生 read_hello 的原始 tool_use。

旁白：

PMA 已经把工具结果和最初调用关联起来。点击来源编号，就会跳回第一次请求的 tool use，并保留返回结果的入口。长会话里，即使结果晚了几轮，也不需要手工搜索调用标识。

## 02:42–03:05　原生 Anthropic Messages 怎样封装这次往返？

画面重点：输入顺序清楚显示 assistant/tool_use 后接 user/tool_result，最后才有模型回答。

旁白：

协议视图保留 Anthropic Messages 的原生角色和顺序。先是用户消息，再是 assistant 文本和 tool use；Claude Code 执行后，把 tool result 放进后续 user message 的 content。模型收到这份新请求，才返回最终回答。

## 03:05–03:27　摘要有疑问，就回到完整请求

画面重点：Raw Inspector 保留原始 headers、body、模型参数、System、Tools 和脱敏记录。

旁白：

如果整理后的协议仍不足以解释问题，可以打开完整请求。Raw Inspector 保留原始请求头、body、模型参数、System、工具定义和消息；敏感请求头会显示脱敏原因，而不是把真实值录进素材。

## 03:27–03:49　最终回答是否真的有工具证据？

画面重点：第二次模型 Response 返回“项目名是 hello-agent”，可以沿时间线向前核对依据。

旁白：

最后点击模型回复的详情。第二次 Response 给出：项目名是 hello agent。现在我们不只看到了答案，还能沿时间线向前核对：它使用了哪一个工具、文件实际返回了什么、结果又怎样进入下一次模型请求。

## 03:49–04:05　模型选择，Claude Code 执行，PMA 留下证据

画面重点：这就是一次完整工具闭环：目标、tool_use、本地执行、tool_result、最终回答。

旁白：

记住这条边界：模型负责根据结构化上下文选择下一步；Claude Code 负责在本地执行和回传；PMA 让每一步都能回到请求、协议和 Raw 证据。下一支视频，我们再拆解 Skill 是怎样被发现和加载的。
